# 2025 高教社杯数学建模 C 题 · 问题 1 完整流程文档

> **本文档给谁看**：队友 / 协作者，照着跑一遍就能拿到全部结果。
> **本文档解决什么**：问题 1 —— 分析胎儿 Y 染色体浓度与孕周、BMI 等指标的相关特性，建立关系模型并检验显著性。
> **数据来源**：`附件.xlsx`（男胎表 1082 行 / 267 位孕妇；女胎表 605 行，问题 1 只用男胎表）。
> **一句话结论**：本质是"**纵向（重复测量）数据的相关性分析 + 关系建模 + 显著性检验**"，不是简单套一个线性回归。


## 1. 输出目录结构（结果按这个组织）

跑完代码后，`25cc` 目录下会生成如下结构，**每个文件职责单一、互不重叠**：

```
25cc/
├── 流程说明.md                ← 本文件（队友先读这个）
├── 附件.xlsx                  ← 原始数据（只读，不要改）
├── code/
│   └── q1_pipeline.py         ← 问题1 主脚本（一键跑全部）
└── output/
    ├── figures/               ← 【图表】所有 PNG 图放这里
    │   ├── fig1_目标变量分布.png
    │   ├── fig2_自变量分布.png
    │   ├── fig3_相关性热力图.png
    │   ├── fig4_Y浓度vs孕周_散点趋势.png
    │   ├── fig5_Y浓度vsBMI_散点趋势.png
    │   ├── fig6_分BMI组趋势.png
    │   ├── fig7_模型拟合对比.png
    │   ├── fig8_预测vs实测.png
    │   ├── fig9_残差QQ图.png
    │   ├── fig10_残差vs拟合值.png
    │   └── fig11_残差直方图.png
    ├── 01_符号设定.md          ← 【符号】全文统一的符号表
    ├── 02_指标汇总.md          ← 【指标】所有统计指标数值结果
    ├── 03_模型选择.md          ← 【模型选择】候选模型对比与最终选择
    ├── 04_分析报告.md          ← 【分析】逐步骤分析结论
    ├── 05_说明与总结.md        ← 【说明+总结】整体说明与最终结论
    └── 交付包/
        └── 问题1_完整交付包.zip   ← 【打包】上面所有内容的压缩包
```

---

## 2. 运行方式

```bash
cd /d/数学建模/25cc
python code/q1_pipeline.py
```

运行完成后：
- 图 → `output/figures/` 下 11 张 PNG
- 文字结果 → `output/` 下 5 个 `.md` 文件
- 打包 → `output/交付包/问题1_完整交付包.zip`

> 约定：**改结果先改代码，改完重跑**，不要手改 `output/` 里的文件，保证可复现。

---

## 3. 完整流程（9 步）

> 每一步：**做什么 → 输入 → 输出**。代码严格按这个顺序写。

### Step 1 数据读取与变量映射
- **做什么**：读 `附件.xlsx` 男胎表，把列名统一成 A~AE 的英文短名。
- **输入**：`附件.xlsx` sheet 0（男胎）。
- **输出**：`DataFrame`（1082 行 × 31 列），打印 shape 与前 5 行。

### Step 2 孕周解析（关键，不做后面全错）
- **做什么**：把文本 `"12w+2"`、`"13w"` 转成数值 `12.286`、`13.0`。
  - 规则：`孕周 = 周数 + 天数 / 7`；无 `+天数` 的按 `周数` 处理。
- **输入**：`J孕周` 列（object 类型）。
- **输出**：新增列 `GA`（浮点，孕周数值）。

### Step 3 数据清洗与特征工程
- **做什么**：
  1. 检查缺失值（Y浓度、孕周、BMI 等核心变量不能有缺失）。
  2. 标记测序质量异常：`GC含量 < 40% 或 > 60%`、`AA过滤比例` 过高、`M比对比例` 异常低 —— 决定剔除还是保留并记录原因。
  3. 构造衍生变量（如 `log_总读段数`、`是否IVF`）。
  4. 记录清洗前后样本量变化。
- **输入**：Step 2 结果。
- **输出**：清洗后的数据 + 清洗说明（写进 `04_分析报告.md`）。

### Step 4 探索性数据分析（EDA）+ 画图
- **做什么**：画分布图、散点趋势图。
- **输出图**：`fig1_目标变量分布`、`fig2_自变量分布`、`fig4_Y浓度vs孕周`、`fig5_Y浓度vsBMI`、`fig6_分BMI组趋势`。
- **产出结论**（写进分析报告）：Y浓度是否右偏、与孕周/BMI 的单调趋势方向。

### Step 5 相关性分析（回答"相关特性"）
- **做什么**：
  1. 计算 Y浓度 与 孕周、BMI、年龄、身高、体重、总读段数、GC含量、过滤比例 等变量的 **Pearson 相关系数 r 及 p 值**。
  2. 画相关性热力图（显著项标星号）。
- **输出图**：`fig3_相关性热力图`。
- **产出数据**：相关系数矩阵 + p 值矩阵 → 写进 `02_指标汇总.md`。

### Step 6 建立关系模型（回答"关系模型"）
- **做什么**：分别拟合下面 5 个候选模型（详见 §6）。
- **输出**：每个模型的系数估计、标准误、t 值、p 值、R²、调整 R²。
- **产出图**：`fig7_模型拟合对比`、`fig8_预测vs实测`。

### Step 7 显著性检验与残差诊断
- **做什么**：
  1. 单系数 **t 检验**（p<0.05 显著）。
  2. 整体 **F 检验**。
  3. **共线性诊断 VIF**（>10 剔除）。
  4. **残差诊断**：正态性（Q-Q / Shapiro）、同方差、独立性。
- **输出图**：`fig9_残差QQ图`、`fig10_残差vs拟合值`、`fig11_残差直方图`。
- **产出数据**：t/F/p/VIF/残差检验 → 写进 `02_指标汇总.md`。

### Step 8 模型选择（回答"哪个模型最好"）
- **做什么**：用 **AIC/BIC + 交叉验证 RMSE** 对比候选模型，选出最终模型。
- **产出数据**：模型对比表 → 写进 `03_模型选择.md`。

### Step 9 汇总、写文档、打包
- **做什么**：把前面所有结果整理成 5 个 md + 图，并压缩打包。
- **输出**：`output/` 下全部文件 + `问题1_完整交付包.zip`。

---

## 4. 符号设定（全文统一，写入 `01_符号设定.md`）

| 符号 | 含义 | 单位/取值 |
|---|---|---|
| $Y$ | 胎儿 Y 染色体浓度（目标变量） | 比例，0.01 ~ 0.23 |
| $GA$ | 孕周 | 周（如 12.286） |
| $BMI$ | 身体质量指数 | kg/m²，20 ~ 47 |
| $Age$ | 孕妇年龄 | 岁 |
| $H$ | 身高 | cm |
| $W$ | 体重 | kg |
| $R$ | 总读段数 | 个 |
| $GC$ | GC 含量 | 0 ~ 1 |
| $\beta_0,\beta_1,\dots$ | 回归系数 | — |
| $\varepsilon$ | 随机误差项 | — |
| $u_i$ | 孕妇 $i$ 的随机截距（混合效应） | — |
| $r$ | Pearson 相关系数 | −1 ~ 1 |
| $p$ | 显著性 p 值 | 0 ~ 1 |
| $R^2$ / $\bar R^2$ | 决定系数 / 调整决定系数 | 0 ~ 1 |
| $t$ | t 统计量 | — |
| $F$ | F 统计量 | — |
| $\mathrm{VIF}$ | 方差膨胀因子 | ≥ 1 |
| $\mathrm{AIC}$ / $\mathrm{BIC}$ | 信息准则 | 越小越好 |
| $\mathrm{RMSE}$ / $\mathrm{MAE}$ | 均方根误差 / 平均绝对误差 | 越小越好 |

> 说明：男胎表列 `V` = $Y$，列 `J` = 孕周（文本），列 `K` = $BMI$，列 `C/D/E` = $Age/H/W$，列 `L` = $R$，列 `P` = $GC$。

---

## 5. 指标定义（写入 `02_指标汇总.md`）

| 指标 | 公式 / 含义 | 判定标准 |
|---|---|---|
| Pearson $r$ | 线性相关程度 | \|r\| 越大相关越强；配合 p<0.05 |
| 回归系数 $\beta$ | 自变量每变化 1 单位，$Y$ 平均变化量 | 置信区间不跨 0 |
| t 值 | $\hat\beta / \mathrm{SE}(\hat\beta)$ | \|t\| 越大越显著 |
| p 值 | 系数显著水平 | < 0.05 显著，< 0.01 极显著 |
| $R^2$ / $\bar R^2$ | 解释方差比例 | 越接近 1 拟合越好 |
| F 值 | 整体回归显著性 | p_F < 0.05 |
| VIF | 共线性程度 | > 10 说明共线性严重 |
| AIC / BIC | 模型复杂度惩罚后的拟合 | 越小越好 |
| RMSE / MAE | 预测误差 | 越小越好 |
| 残差正态性 | Shapiro-Wilk / Q-Q | p > 0.05 满足正态 |

---

## 6. 模型选择（写入 `03_模型选择.md`）

### 候选模型（5 个）

| 编号 | 模型 | 形式 | 用途 |
|---|---|---|---|
| M1 | 基准线性 | $Y = \beta_0 + \beta_1 GA + \beta_2 BMI + \varepsilon$ | 打底 |
| M2 | 多元线性 | M1 + $Age$（视 VIF 决定是否加身高体重） | 看"等指标" |
| M3 | 非线性 | M1 + $\beta_3 GA^2$，或 $\ln Y = \beta_0 + \beta_1 \ln GA + \beta_2 BMI + \varepsilon$ | 捕捉饱和趋势 |
| M4 | 交互项 | M1 + $\beta_3 (GA \times BMI)$ | 检验 BMI 是否改变斜率 |
| M5 | 混合效应 | $Y_{ij} = \beta_0 + \beta_1 GA_{ij} + \beta_2 BMI_i + u_i + \varepsilon_{ij}$ | 处理重复测量 |

### 选择规则（按优先级）

1. 残差诊断必须通过（正态、同方差）。
2. AIC / BIC 最小。
3. 交叉验证 RMSE 最小。
4. 若 M5 的组内相关显著（$u_i$ 方差大），优先选 M5 作为最终结论，其余作对照。

---

## 7. 分析框架（写入 `04_分析报告.md`）

每一步要回答的问题 + 结论写法：

1. **相关特性**：Y浓度 与孕周 **正相关**、与 BMI **负相关**（附 r 与 p），年龄/身高等是否显著。
2. **关系模型**：最终模型形式 + 各系数含义（"孕周每增 1 周，Y浓度平均 +X；BMI 每增 1 单位，Y浓度平均 −X"）。
3. **显著性**：t/F/p/VIF/残差诊断结果，说明模型可信。
4. **物理解释**：用生物学机制解释 —— 胎儿分数随孕周升高、随母体 BMI 稀释下降。

---

## 8. 打包交付（写入 `05_说明与总结.md` 并在最后生成 zip）

- 汇总：目标 → 方法 → 结果 → 结论，一段话讲清问题 1 全貌。
- 打包命令（在脚本末尾自动执行）：

```python
import shutil
shutil.make_archive('output/交付包/问题1_完整交付包', 'zip', 'output')
```

---

## 9. 交付检查清单（跑完逐项打勾）

- [ ] `figures/` 下 11 张图全部生成且能打开
- [ ] 5 个 `.md` 文件内容齐全、无空文件
- [ ] `02_指标汇总.md` 里 p 值、R²、AIC/BIC 都有数值
- [ ] `03_模型选择.md` 里有最终模型结论
- [ ] zip 包能解压、包含全部 output 内容
- [ ] 论文里引用的符号与 `01_符号设定.md` 一致

---

*本流程对应问题 1；问题 2~4 可在同框架下扩展（问题 2/3 加"达标时间建模"，问题 4 换女胎表做分类）。*
